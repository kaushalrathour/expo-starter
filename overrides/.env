# Environment Variables for Expo App
# 
# IMPORTANT: All environment variables that should be accessible in your app 
# MUST be prefixed with "EXPO_PUBLIC_" to be available at runtime.
#
# Variables without this prefix are only available during build time and 
# will NOT be accessible in your React Native code.

# ✅ These variables will be available in your app via process.env
EXPO_PUBLIC_API_URL=https://api.example.com
EXPO_PUBLIC_APP_NAME=ExpoStarter
EXPO_PUBLIC_DEBUG_MODE=false
EXPO_PUBLIC_ANALYTICS_ID=GA-XXXXXXXXX
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# ❌ These variables will NOT be available in your app (build-time only)
# API_SECRET_KEY=secret-key-here
# DATABASE_PASSWORD=super-secret-password

# 📖 How to use these variables in your code:
# 
# import { Text } from 'react-native';
# 
# export default function MyComponent() {
#   const apiUrl = process.env.EXPO_PUBLIC_API_URL;
#   const appName = process.env.EXPO_PUBLIC_APP_NAME;
#   const isDebug = process.env.EXPO_PUBLIC_DEBUG_MODE === 'true';
#   
#   return <Text>API URL: {apiUrl}</Text>;
# }
#
# 🔐 Security Note:
# Never put sensitive information (API keys, secrets, passwords) in 
# EXPO_PUBLIC_ variables as they will be bundled with your app and 
# visible to anyone who downloads it.

# 🚀 For production builds:
# Copy this file to .env.production and update values accordingly
# Expo will automatically use the appropriate .env file based on your build configuration
